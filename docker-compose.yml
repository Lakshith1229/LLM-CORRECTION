version: '3.8'

services:
  ocr-app:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./uploads:/app/uploads
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
      - UPLOAD_FOLDER=/app/uploads
    restart: unless-stopped
